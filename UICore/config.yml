targets:

  ############
  # Framework
  ############
  
  UICore:
    name: UICore
    type: framework
    platform: iOS

    settings:
      PRODUCT_BUNDLE_IDENTIFIER: "com.myapp.uicore"
      INFOPLIST_FILE: UICore/Configs/Info.plist

    # dependencies:
    
    sources:
      - path: Sources
        group: Modules/UICore
      - path: Configs/Info.plist
        group: Modules/UICore
